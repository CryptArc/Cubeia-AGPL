IMPORTANT:

Not all clients are in synch with the actual server code.

The web (HTML5) and Java (cmd-line) clients are usually up to date.

The Flash (flex) and javascript clients are provided for reference only. They
are currently not maintained.

A typical setup uses the web-client.