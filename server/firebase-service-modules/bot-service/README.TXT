This is a service that will start an embedded (within Firebase) jetty for
serving bots.

The bot-server source is located at server/bot-modules/bot.

After starting the poker game server, the bot-server can be reached at:

http://localhost:19090
