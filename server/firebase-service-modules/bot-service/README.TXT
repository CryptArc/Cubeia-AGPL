This is a service that will start an embedded (within Firebase) jetty for serving bots. It currently uses the port
19090.

It existed to make it easier to start bots, but with the Maven plugin for starting bots, it should now be easy enough, so the
value of this service is questionable.
