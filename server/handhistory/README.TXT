This is the root of the hand history service.

It consists of two parts, the api and the service implementation. Should you wish to provide your own implementation
you can depend on the hh-api module for some reusable domain entities.

The actual service contract that should be implemented is located (somewhat unorthodoxly) in the poker-game module.